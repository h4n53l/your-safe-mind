import ServerComponentsSpinner from "@/components/server-components-spinner";
import React from "react";

function Loading() {
  return (
    <ServerComponentsSpinner />
  );
}

export default Loading;